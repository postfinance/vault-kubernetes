export CLUSTER="k8s-lab"
export APISERVER="https://${CLUSTER}.example.com"
export TRUSTSTORE="<path to truststore.pem>"
export NAMESPACE="vault-dev"
export SERVICEACCOUNT="vault-auth"

export AUTHENTICATOR_IMAGE="vault-kubernetes-authenticator:x.y.z"
export SYNCHRONIZER_IMAGE="vault-kubernetes-synchronizer:x.y.z"
export TOKEN_RENEWER_IMAGE="vault-kubernetes-token-renewer:x.y.z"
export IMAGE="gcr.io/kuar-demo/kuard-amd64:1"

export VAULT_ADDR="http://vault-dev-server.vault-dev.svc.cluster.local:8200"
export VAULT_K8S_MOUNT_PATH="auth/${CLUSTER}"
export VAULT_ROLE="${CLUSTER}-${NAMESPACE}-auth"
export VAULT_TOKEN_DIR="/home/vault"
export VAULT_TOKEN_PATH="${VAULT_TOKEN_DIR}/.vault-token"
